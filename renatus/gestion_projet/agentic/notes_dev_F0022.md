# Notes dev F0022 — pictogrammes types

## Pictos (SVG 24x24)

| Type | Motif |
|------|--------|
| dataframe | fichier + lignes |
| table | grille |
| view | oeil |
| execute | chevron terminal |
| iteration | boucle refresh |

## Emplacements

- Palette outils (pastille coloree + icone)
- Noeuds du graphe (icone a gauche du nom)
- Liste requires (icone + type)

## Fichiers

- `app.js`: `typeIconPaths`, `typeIconSvg`, `typeIconSvgGroup`
- `style.css`: `.tool-icon-wrap`, `.node-icon`, couleurs par type
