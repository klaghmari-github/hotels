# F0055

Feature. Ajouter un composant execute python qui permet d executer un code python  dans le .venv par defaut du projet ou dans un venv selectionné par le user dans la config du composant il faut une zone script. Pour executer le python

Source: web console
