t_natures:
  type: table
  mode: create_if_not_exists
  requires:
  - t_sales
  script: 'SELECT DISTINCT NATURE_PRODUIT AS NATURE

    FROM t_sales

    WHERE NATURE_PRODUIT IS NOT NULL

    ORDER BY NATURE

    '
