t_dataset_mix_nombre_detail:
  type: table
  mode: create_or_replace
  requires:
  - v_dataset_mix_nombre_long
  script: 'PIVOT v_dataset_mix_nombre_long

    ON metric_name

    USING MAX(metric_value)

    GROUP BY hotel_code

    '
  name: t_dataset_mix_nombre_detail
