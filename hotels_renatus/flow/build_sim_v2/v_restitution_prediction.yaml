v_restitution_prediction:
  type: view
  mode: create_or_replace
  requires:
  - v_restitution_prediction_core
  - v_solution_conversion_rate
  script: "SELECT\n  p.*,\n  c.taux_conversion_moyen,\n  c.taux_conversion_pondere,\n\
    \  c.nombre_hotels AS nombre_hotels_conversion\nFROM v_restitution_prediction_core\
    \ AS p\nLEFT JOIN v_solution_conversion_rate AS c USING (solution)\nORDER BY\n\
    \  CASE lower(cast(solution AS VARCHAR))\n    WHEN 'connected' THEN 0\n    WHEN\
    \ 'liberty' THEN 1\n    WHEN 'simply' THEN 2\n    ELSE 9\n  END,\n  solution"
  name: v_restitution_prediction
