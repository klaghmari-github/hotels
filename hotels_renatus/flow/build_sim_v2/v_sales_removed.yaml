v_sales_removed:
  type: view
  mode: create_or_replace
  requires:
  - t_sales
  - v_scenario_removed_natures
  script: 'SELECT s.*

    FROM t_sales AS s

    INNER JOIN v_scenario_removed_natures AS r ON r.nature = s.NATURE_PRODUIT

    '
  name: v_sales_removed
