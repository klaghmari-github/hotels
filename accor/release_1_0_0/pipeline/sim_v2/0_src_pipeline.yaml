df_sales:
  type: dataframe
  requires: []
  file: data/files/input/hotel_sales_raw_extended_data.xlsx
t_sales:
  type: table
  mode: create_if_not_exists
  requires:
  - df_sales
  sql: |-
    SELECT
        *
    FROM
        df_sales
    WHERE
        PRIX_TTC > 0
        AND MARGE > 0
